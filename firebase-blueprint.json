{
  "entities": {
    "FestivalDatabase": {
      "title": "Festival Database State",
      "description": "Full application state for programs, participants, results, teams, settings, and notices",
      "type": "object",
      "properties": {
        "teams": { "type": "array" },
        "programs": { "type": "array" },
        "participants": { "type": "array" },
        "results": { "type": "array" },
        "settings": { "type": "object" },
        "prevRanks": { "type": "object" },
        "lastModified": { "type": "number" }
      }
    }
  },
  "firestore": {
    "/appState/current": {
      "schema": { "$ref": "#/entities/FestivalDatabase" },
      "description": "Shared live application state for real-time syncing"
    }
  }
}
